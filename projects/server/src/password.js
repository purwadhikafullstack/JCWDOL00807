const { hashPassword, hashMatch, saltSecret } = require("./lib/hash");

// let password = "admin123"
// console.log(await hashPassword(password))

// const createPassword = async(p) => {
//    return password = await hashPassword(p)
    
// }
// console.log(createPassword(password))

